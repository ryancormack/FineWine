@model IEnumerable<FineWine.Domain.Model.Wine>
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div>
        yolo
        @if (Model != null)
        {
            foreach (var wine in Model)
            {
                @wine.Name
            }
        }
        else
        {
            <h3>Seems the Mongo server is not running! Please check and try again.</h3>
        }
    </div>
</body>
</html>
